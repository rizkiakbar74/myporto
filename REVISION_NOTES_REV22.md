REV22 updates:
- Header remains fixed at the top and follows the user while scrolling.
- Header gains a softer scrolled state when the page moves down.
- Looping/continuous motions are now gated with useInView.
- Hero floating words, icon motions, scan effects, and orbit rings only run while the hero section is visible.
- About orbit animation only runs while the About section is visible.
- Gallery marquee only moves while the Gallery section is visible.
- Global cinematic background no longer has always-running infinite loop animation.
- Section reveal animations still trigger when each section enters viewport.

- Header icon no longer loops endlessly; it only reacts on hover.
- Hero typewriter pauses when Hero is outside the viewport.
